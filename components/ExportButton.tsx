import { Button } from "react-native";

export default function ExportButton({ disabled }) {
  return (
    <Button
      title="Export Attendance"
      disabled={disabled}
      onPress={() => {}}
    />
  );
}
